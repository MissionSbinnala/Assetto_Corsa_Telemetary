<UserControl x:Class="Telemetry.UserControls.StintRecordExpander"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Telemetry.UserControls"
             xmlns:converter="clr-namespace:Telemetry.Converter"
             mc:Ignorable="d" 
             
             xmlns:ui="http://schemas.modernwpf.com/2019"
             
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converter:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:StintRecordViewModel x:Key="StintRecordViewModel"/>
    </UserControl.Resources>
    <Expander Background="#ffd9e6" Header="数据选择" Name="Expander" IsExpanded="True" DataContext="{StaticResource StintRecordViewModel}">
        <StackPanel Name="Gaps">

            <Button Padding="24,0,24,0" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" MinHeight="40" Style="{StaticResource MaterialDesignPaperButton}">Data 1</Button>
            <Button Padding="24,0,24,0" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" MinHeight="40" Style="{StaticResource MaterialDesignPaperButton}">Data 2</Button>





            <Button Padding="24,0,24,0" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" MinHeight="40" Style="{StaticResource MaterialDesignPaperButton}">Data 3</Button>
            <Button Padding="24,0,24,0" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" MinHeight="40" Style="{StaticResource MaterialDesignPaperButton}">Data 4</Button>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="64"/>
                </Grid.ColumnDefinitions>
                <Button Grid.ColumnSpan="2"  Padding="24,0,24,0" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" MinHeight="40" Style="{StaticResource MaterialDesignPaperButton}" Cursor="Hand" Content="{Binding Name}" Command="{Binding Select}"/>
                <Canvas Grid.Column="1" Width="24" Height="24" Visibility="{Binding IsActive ,Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Path Stroke="Gray" StrokeThickness="2" StrokeLineJoin="Round" Data="M 4 12 L 10 18 L 20 6"/>
                </Canvas>
            </Grid>

            <Button Margin="24, 12, 24, 12" Style="{ui:StaticResource ResourceKey=DefaultButtonStyle}" HorizontalAlignment="Stretch" Background="AliceBlue" MinHeight="40" Cursor="">Temp</Button>
            <CheckBox Margin="24, 12, 24, 12" Style="{StaticResource MaterialDesignCheckBox}">I need to check</CheckBox>





        </StackPanel>
    </Expander>

</UserControl>
