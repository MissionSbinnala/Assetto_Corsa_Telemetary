<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Telemetry.UserControls"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"         
             xmlns:Converter="clr-namespace:Telemetry.Converter" x:Class="Telemetry.UserControls.ChartDisplaysTest"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Converter:PercentageConverter x:Key="PercentageConverter"/>
    </UserControl.Resources>
    <Grid x:Name="grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="ButtonLeft" Grid.Column="0" 
                Width="{Binding ActualWidth, 
                ElementName=grid, Mode=OneWay,
                Converter={StaticResource PercentageConverter},
                ConverterParameter=0.167}"/>

        <lvc:CartesianChart Grid.Column="1" 
                DataContext="{Binding chartsViewModel}"
                Series="{Binding AllLines}"/>

        <Button x:Name="ButtonRight" Grid.Column="2" 
                Width="{Binding ActualWidth, 
                ElementName=grid, Mode=OneWay,
                Converter={StaticResource PercentageConverter},
                ConverterParameter=0.167}"
                DataContext="{Binding chartsViewModel}"
                Command="{Binding Increase}"
                />

    </Grid>
</UserControl>
