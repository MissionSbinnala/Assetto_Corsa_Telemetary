<Page x:Class="Telemetry.Pages.StintPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Telemetry.Pages"
      mc:Ignorable="d" 
      
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      
      xmlns:viewmodel="clr-namespace:Telemetry.ViewModel" 
      d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
      
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StintPage">

    <Grid>
        <TabControl>
            <TabItem Header="Pedals" x:Name="PedalTab">
                <lvc:CartesianChart Series="{Binding AllLines[0]}"/>
            </TabItem>
            <TabItem Header="Tyres" x:Name="TyresTab">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <lvc:CartesianChart Padding="2"  Series="{Binding AllLines[0]}" Grid.Row="0" Grid.Column="0"/>
                    <lvc:CartesianChart Padding="2"  Series="{Binding AllLines[1]}" Grid.Row="0" Grid.Column="1"/>
                    <lvc:CartesianChart Padding="2"  Series="{Binding AllLines[2]}" Grid.Row="1" Grid.Column="0"/>
                    <lvc:CartesianChart Padding="2"  Series="{Binding AllLines[3]}" Grid.Row="1" Grid.Column="1"/>
                </Grid>
            </TabItem>
            <TabItem Header="Wheels"  x:Name="WheelsTab">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <lvc:CartesianChart Padding="2"  Series="{Binding AllLines[0]}" Grid.Row="0" Grid.Column="0"/>
                    <lvc:CartesianChart Padding="2"  Series="{Binding AllLines[1]}" Grid.Row="0" Grid.Column="1"/>
                    <lvc:CartesianChart Padding="2"  Series="{Binding AllLines[2]}" Grid.Row="1" Grid.Column="0"/>
                    <lvc:CartesianChart Padding="2"  Series="{Binding AllLines[3]}" Grid.Row="1" Grid.Column="1"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
