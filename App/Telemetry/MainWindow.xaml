<Window x:Class="Telemetry.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions" 
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        x:Name="MW"
        
        xmlns:local="clr-namespace:Telemetry"
        xmlns:vw="clr-namespace:Telemetry.ViewModel"
        xmlns:uc="clr-namespace:Telemetry.UserControls"
        xmlns:cc="clr-namespace:Telemetry.ContentControls"
        xmlns:converter="clr-namespace:Telemetry.Converter"
        xmlns:pages="clr-namespace:Telemetry.Pages"
        
        xmlns:lib="clr-namespace:SlickLibrary;assembly=SlickLibrary"
        
        d:DataContext="{d:DesignInstance Type=vw:MainViewModel}"
        
        Title="MainWindow" Height="450" Width="800">
    <Grid ClipToBounds="False">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <lib:SidePanel Grid.RowSpan="2" Grid.ColumnSpan="3" Panel.ZIndex="10" InWindow="{Binding ElementName=MW}">
            <StackPanel>
                <lib:IconButton ButtonContent="StintRecord" Command="{Binding SwitchToPageZeroCommand}"/>
                <lib:IconButton ButtonContent="Current" Command="{Binding SwitchToPageOneCommand}"/>
            </StackPanel>
        </lib:SidePanel>

        <Frame Source="{Binding DataContext,ElementName=MW}" x:Name="MainFrame" Grid.Row="0" Grid.Column="1" NavigationUIVisibility="Hidden" Visibility="Visible"/>

    </Grid>
</Window>
